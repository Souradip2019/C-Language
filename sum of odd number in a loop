#include <stdio.h>
int main ()
{
    int range,sum=0,i;
    printf ("Enter the range: ");
    scanf ("%d",&range);
    for (i=1;i<=range;i++)
    {
        if (i%2!=0)
        {
         sum=sum+i;
        }
    }
        printf ("SUM=%d",sum);
    return 0;
}
