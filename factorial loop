#include <stdio.h>
int main()
{
   int num,i,ans=1;
   printf ("Enter the number: ");
   scanf ("%d",&num);
   for (i=num;i>=1;i--)
    ans=ans*i;
   printf ("ans=%d",ans);
   return 0;
   getch ();
}
